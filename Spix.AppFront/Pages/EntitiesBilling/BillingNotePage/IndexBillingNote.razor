@page "/billingnotes"
@layout MainLayout
@attribute [Authorize(Roles = "Administrator")]

@using Spix.Domain.EntitiesBilling

<div class="container-fluid pt-4">
    <div class="card shadow-lg p-4 mb-5" style="border-radius: 12px;">

        @* Encabezado: el unico titulo de la pantalla *@
        <div class="idx-head">
            <span class="idx-glifo"><i class="fa fa-calendar-alt"></i></span>
            <div class="idx-head-text">
                <h3>@Localizer["BillingNote_Title"]</h3>
                <p>@Localizer["BillingNote_Subtitle"]</p>
            </div>
        </div>

        @* El tablero: lo cuenta la base *@
        @if (Summary is not null)
        {
            <div class="bn-board">
                <div class="bn-tile">
                    <div class="bn-tile-label">@Localizer["BillingNote_Year", Summary.YearNumber]</div>
                    <div class="bn-tile-value">@Summary.Notes.ToString("N0")</div>
                </div>
                <div class="bn-tile is-done">
                    <div class="bn-tile-label">@Localizer["BillingNote_Launched"]</div>
                    <div class="bn-tile-value">@Summary.Launched.ToString("N0")</div>
                </div>
                <div class="bn-tile is-open">
                    <div class="bn-tile-label">@Localizer["BillingNote_PendingNotes"]</div>
                    <div class="bn-tile-value">@Summary.Pending.ToString("N0")</div>
                </div>
                <div class="bn-tile is-client">
                    <div class="bn-tile-label">@Localizer["BillingNote_ActiveContracts"]</div>
                    <div class="bn-tile-value">@Summary.ActiveContracts.ToString("N0")</div>
                </div>
            </div>
        }

        <div class="idx-bar">
            <ButtonFormNew OnClick="ShowModalAsync" />
            <div class="idx-search">
                <FilterComponent ApplyFilter="SetFilterValue" />
            </div>
        </div>

        <section class="idx-panel">
            <div class="table-responsive">
                <table class="idx-table">
                    <thead>
                        <tr>
                            <th>@Localizer["BillingNote_Period"]</th>
                            <th>@Localizer[nameof(Resource.Status)]</th>
                            <th style="text-align:right">@Localizer["Table_Actions"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        <GenericList MyList="BillingNotes">
                            <Loading>
                                <tr>
                                    <td colspan="3" class="idx-meta" style="text-align:center">
                                        <span class="spinner-border spinner-border-sm me-2"></span>@Localizer["Table_Loading"]
                                    </td>
                                </tr>
                            </Loading>
                            <NoRecords>
                                <tr>
                                    <td colspan="3" class="idx-meta" style="text-align:center">@Localizer["Table_NoRecords"]</td>
                                </tr>
                            </NoRecords>
                            <Body>
                                @foreach (var item in BillingNotes!)
                                {
                                    <tr>
                                        <td>
                                            <div class="idx-name">@GetMonthName(item.MonthType) @item.YearNumber</div>
                                            <div class="idx-meta">@item.DateBill.ToString("dd/MM/yyyy")</div>
                                        </td>
                                        <td>
                                            @if (item.Created)
                                            {
                                                <span class="idx-pill is-on">@Localizer["BillingNote_Launched"]</span>
                                                <div class="idx-meta">@item.DateCreated?.ToString("dd/MM/yyyy")</div>
                                            }
                                            else
                                            {
                                                <span class="idx-pill is-off">@Localizer["BillingNote_Pending"]</span>
                                            }
                                        </td>
                                        <td>
                                            <div class="idx-actions">
                                                <ButtonGeneric OnClick="() => ShowDetailsAsync(item.BillingNoteId)"
                                                               TooTip="@Localizer["CxC_Details"]"
                                                               Icon="fa fa-list-alt" />
                                                @if (!item.Created)
                                                {
                                                    <ButtonEdit OnClick="() => ShowEditAsync(item.BillingNoteId)" />
                                                    <ButtonDelete OnClick="() => DeleteAsync(item.BillingNoteId)" />
                                                }
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </Body>
                        </GenericList>
                    </tbody>
                </table>
            </div>

            @if (BillingNotes is not null)
            {
                <div class="idx-panel-foot">
                    <Pagination CurrentPage="CurrentPage"
                                SelectedPage="SelectedPage"
                                TotalPages="TotalPages" />
                </div>
            }
        </section>
    </div>
</div>
