<EditForm Model="ProductCategory" OnValidSubmit="OnSubmit">
    <DataAnnotationsValidator />
    <MudTextField Label="@GetDisplayName(() => ProductCategory.Name)"
                  @bind-Value="@ProductCategory.Name"
                  For="@(() => ProductCategory.Name)"
                  Class="mb-4" />
    <MudCheckBox Label="@GetDisplayName(() => ProductCategory.Active)"
                 @bind-Value="@ProductCategory.Active"
                 For="@(() => ProductCategory.Active)"
                 Class="mb-4" />
    <br />
    <div class="mb-4" style="display: flex; justify-content: center; gap: 10px; margin-top: 10px;">
        <MudButton Variant="Variant.Filled"
                   StartIcon="@Icons.Material.Filled.ArrowBack"
                   Color="Color.Surface"
                   OnClick="ReturnAction">
            Volver
        </MudButton>
        <MudButton Variant="Variant.Filled"
                   StartIcon="@Icons.Material.Filled.Save"
                   Color="Color.Dark"
                   ButtonType="ButtonType.Submit">
            Guardar
        </MudButton>
    </div>
</EditForm>
