@page "/plans/Createproducts/{Id:guid}"
@attribute [Authorize(Roles = "Usuario")]

@if (Plan is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <FormPlan Plan="Plan"
                      IsEditControl="false"
                      OnSubmit="Create"
                      ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}