@page "/marks/create"
@attribute [Authorize(Roles = "Usuario")]

@if (Mark is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <FormMark Mark="Mark"
                      OnSubmit="Create"
                      ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
