@page "/login"
@layout LoginLayout

@* Mientras se verifica la sesion no se muestra el formulario: si ya hay usuario logueado
   se redirige a su dashboard sin que el login alcance a parpadear. *@
@if (_checkingAuth)
{
    <AppBootScreen />
}
else
{
    <Login />
}
