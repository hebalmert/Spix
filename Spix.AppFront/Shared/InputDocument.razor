<div class="input-group">
    <span class="input-group-text" style="width: 150px">
        @Label
    </span>
    <input type="text"
           class="form-control"
           placeholder="Seleccionar archivo"
           readonly
           value="@FileName" />
    <label class="input-group-btn">
        <span class="btn btn-dark px-3">
            Search
            <InputFile OnChange="OnChange" class="d-none" accept="@AcceptExtensions" />
        </span>
    </label>
</div>

@if (!string.IsNullOrWhiteSpace(FileName))
{
    <div class="mt-2 text-muted text-truncate">
        File: <strong>@FileName</strong>
    </div>
}
@if (!string.IsNullOrEmpty(CurrentPreviewUrl))
{
    <div class="mt-3 d-flex justify-content-center">
        <iframe src="@CurrentPreviewUrl"
                class="pdf-thumb-frame"
                style="border:1px solid #ddd; border-radius: 8px; width:100%; max-width:520px; height:420px;">
        </iframe>
    </div>
}
