@inherits LayoutComponentBase


<article class="authFull spix-atmosphere">
    @Body
</article>

@* Los modales van FUERA de la pagina, en una capa que cubre toda la pantalla (ver .spix-modal-host) *@
@if (modalType is not null)
{
    <ModalHost ComponentType="@modalType" Parameters="@modalParameters" />
}
