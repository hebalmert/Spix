<UserControl x:Class="Spix.AppWpf.SharedComponents.SharedSearchFilter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="clr-namespace:FontAwesome.Net.Wpf;assembly=FontAwesome.Net.Wpf"
             xmlns:icons="clr-namespace:FontAwesome.Net.Generators"
             MinWidth="640">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border Height="40" Background="#FFFFFF" BorderBrush="#566273" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <fa:FontAwesomeImage Width="15" Height="15" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#48576B"
                                     Icon="{x:Static icons:FontAwesomeIcon.MagnifyingGlass}"
                                     IconStyle="{x:Static icons:FontAwesomeIconStyle.Solid}" />
                <TextBox Grid.Column="1"
                         Margin="0,1,12,1"
                         MinWidth="260"
                         BorderThickness="0"
                         Background="Transparent"
                         FontSize="14"
                         Foreground="#1E2B3C"
                         VerticalContentAlignment="Center"
                         ToolTip="Buscar"
                         Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </Border>

        <Button Grid.Column="2" Content="Buscar" Command="{Binding SearchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Style="{StaticResource SharedButtonFormPrimary}" />
        <Button Grid.Column="4" Content="Limpiar" Command="{Binding ClearCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Style="{StaticResource SharedButtonFormSecondary}" />
    </Grid>
</UserControl>
