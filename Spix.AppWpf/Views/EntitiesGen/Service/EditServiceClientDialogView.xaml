<UserControl x:Class="Spix.AppWpf.Views.EntitiesGen.Service.EditServiceClientDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:Spix.AppWpf.SharedServices"
             xmlns:fa="clr-namespace:FontAwesome.Net.Wpf;assembly=FontAwesome.Net.Wpf"
             xmlns:icons="clr-namespace:FontAwesome.Net.Generators"
             xmlns:local="clr-namespace:Spix.AppWpf.Views.EntitiesGen.Service"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="28" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:ServiceClientFormView />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding CancelCommand}" Style="{StaticResource SharedButtonFormSecondary}"
                    svc:FormButton.Icon="{x:Static icons:FontAwesomeIcon.ArrowLeft}"
                    Content="Volver" />
            <Button Grid.Column="2" Command="{Binding SaveCommand}" Style="{StaticResource SharedButtonFormSuccess}"
                    svc:FormButton.Icon="{x:Static icons:FontAwesomeIcon.FloppyDisk}"
                    Content="Guardar" />
        </Grid>
        <shared:SharedLoading Grid.RowSpan="3" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
