using System.Windows.Controls;

namespace Spix.AppWpf.Views.EntitiesNet.IpNet;

// Agrupa los campos reutilizados por crear y editar una direccion IP de cliente.
public partial class IpNetFormView : UserControl
{
    public IpNetFormView()
    {
        InitializeComponent();
    }
}
