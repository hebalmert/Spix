<UserControl x:Class="Spix.AppWpf.Views.EntitiesSchedule.ScheduleIndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:calendar="clr-namespace:Spix.AppWpf.SharedComponents.SharedCalendar"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="13"
                   Foreground="#FF9AAE"
                   Text="{Binding Message}"
                   Visibility="{Binding HasMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <Grid Grid.Row="2">
            <calendar:SharedFullCalendarView x:Name="CalendarView" />
            <shared:SharedLoading IsLoading="{Binding IsLoading}" />
        </Grid>
    </Grid>
</UserControl>
