<UserControl x:Class="Spix.AppWpf.Views.EntitiesSchedule.ScheduleIndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="clr-namespace:FontAwesome.Net.Wpf;assembly=FontAwesome.Net.Wpf"
             xmlns:icons="clr-namespace:FontAwesome.Net.Generators"
             xmlns:calendar="clr-namespace:Spix.AppWpf.SharedComponents.SharedCalendar"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents">

    <!-- La agenda, replicada de /schedule de la web: el filtro por estado, la leyenda de
         colores y el calendario. Cada cita se pinta del color de SU estado, asi que la
         leyenda es lo que hace legible el calendario de un golpe de vista. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource SharedIndexShell}" Padding="16,14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Filtrar por estado: cero es "todos" -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource SharedIndexName}" FontSize="16" Text="Agenda" />
                        <TextBlock Style="{StaticResource SharedIndexMeta}"
                                   Text="Las visitas del equipo. El color dice en que va cada una." />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <fa:FontAwesomeImage Width="14" Height="14"
                                             Margin="0,0,10,0"
                                             VerticalAlignment="Center"
                                             Foreground="{DynamicResource BrushIndexIconEdit}"
                                             Icon="{x:Static icons:FontAwesomeIcon.Filter}"
                                             IconStyle="{x:Static icons:FontAwesomeIconStyle.Solid}" />

                        <ComboBox Width="240"
                                  Style="{StaticResource SharedFormCombo}"
                                  ItemsSource="{Binding Statuses}"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding StatusFilter, Mode=TwoWay}" />
                    </StackPanel>
                </Grid>

                <!-- Leyenda: que significa cada color del calendario -->
                <ItemsControl Grid.Row="1"
                              Margin="0,14,0,0"
                              ItemsSource="{Binding Legend}"
                              Visibility="{Binding HasLegend, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,8,6"
                                    Padding="10,5"
                                    CornerRadius="999"
                                    Background="{DynamicResource BrushWhenNextBack}"
                                    BorderBrush="{DynamicResource BrushCatalogKpiBorder}"
                                    BorderThickness="1">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10"
                                             VerticalAlignment="Center"
                                             Fill="{Binding Color}" />
                                    <TextBlock Margin="8,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="12"
                                               Foreground="{DynamicResource BrushWhenNextText}"
                                               Text="{Binding Name}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Grid.Row="2"
                           Margin="0,10,0,0"
                           Style="{StaticResource SharedCatalogMessage}"
                           Text="{Binding Message}"
                           Visibility="{Binding HasMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <calendar:SharedFullCalendarView x:Name="CalendarView" />
            <shared:SharedLoading IsLoading="{Binding IsLoading}" />
        </Grid>
    </Grid>
</UserControl>
