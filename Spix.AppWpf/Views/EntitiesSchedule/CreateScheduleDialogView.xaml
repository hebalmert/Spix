<UserControl x:Class="Spix.AppWpf.Views.EntitiesSchedule.CreateScheduleDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Spix.AppWpf.Views.EntitiesSchedule"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents"
             MinWidth="760">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="28" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:ScheduleFormView />
        <shared:SharedModalActions Grid.Row="2"
                                   CancelCommand="{Binding CancelCommand}"
                                   SaveCommand="{Binding SaveCommand}" />
        <shared:SharedLoading Grid.RowSpan="3" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
