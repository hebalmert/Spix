<UserControl x:Class="Spix.AppWpf.Views.EntitiesSchedule.EditScheduleDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:Spix.AppWpf.SharedServices"
             xmlns:fa="clr-namespace:FontAwesome.Net.Wpf;assembly=FontAwesome.Net.Wpf"
             xmlns:icons="clr-namespace:FontAwesome.Net.Generators"
             xmlns:local="clr-namespace:Spix.AppWpf.Views.EntitiesSchedule"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents"
             MinWidth="760">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="28" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:ScheduleFormView />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <shared:SharedModalActions SaveCommand="{Binding SaveCommand}"
                                       CancelCommand="{Binding CancelCommand}">
                <!-- El boton propio de este modal: eliminar la cita -->
                <shared:SharedModalActions.Extra>
                    <Button Command="{Binding DeleteCommand}"
                            Style="{StaticResource SharedButtonFormDanger}"
                            svc:FormButton.Icon="{x:Static icons:FontAwesomeIcon.TrashCan}"
                            Content="Eliminar" />
                </shared:SharedModalActions.Extra>
            </shared:SharedModalActions>
        </Grid>
        <shared:SharedLoading Grid.RowSpan="3" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
