<UserControl x:Class="Spix.AppWpf.Views.EntitiesInven.Product.ProductStockDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:Spix.AppWpf.SharedServices"
             xmlns:icons="clr-namespace:FontAwesome.Net.Generators"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents"
             MinWidth="560">
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource SharedCatalogTitle}" Text="{Binding ProductName}" />

        <Border Grid.Row="2" Style="{StaticResource SharedCatalogPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Style="{StaticResource SharedCatalogPanelHead}">
                    <TextBlock Style="{StaticResource SharedCatalogPanelHeadText}" Text="Existencias por bodega" />
                </Border>

                <DataGrid Grid.Row="1"
                          MaxHeight="320"
                          ItemsSource="{Binding Stocks}"
                          Style="{StaticResource SharedCatalogGrid}"
                          ColumnHeaderStyle="{StaticResource SharedCatalogHeader}"
                          RowStyle="{StaticResource SharedCatalogRow}"
                          CellStyle="{StaticResource SharedCatalogCell}">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="*" Header="Bodega">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource SharedCatalogItemName}"
                                               Text="{Binding ProductStorage.StorageName}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Width="140" Header="Existencia">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource SharedCatalogItemName}"
                                               Text="{Binding Stock, StringFormat=N2}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Este modal solo consulta: el unico boton es el de volver -->
        <Button Grid.Row="4"
                HorizontalAlignment="Right"
                Command="{Binding CloseCommand}"
                Style="{StaticResource SharedButtonFormSecondary}"
                svc:FormButton.Icon="{x:Static icons:FontAwesomeIcon.ArrowLeft}"
                Content="Volver" />

        <shared:SharedLoading Grid.RowSpan="5" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
