<UserControl x:Class="Spix.AppWpf.Views.EntitiesContratos.ContractClient.ContractIdPicsDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:icons="clr-namespace:FontAwesome.Net.Generators"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents"
             MinWidth="800">

    <!-- Las dos caras del documento del cliente. Del disco o de la camara: es el mismo
         selector del cliente y del proveedor, solo cambia el icono. -->
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="26" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <shared:SharedImagePicker HorizontalAlignment="Center"
                                  Title="Frente"
                                  ImageUrl="{Binding Entity.ImageFrontFullPath}"
                                  Icon="{x:Static icons:FontAwesomeIcon.IdCard}"
                                  ImageSelected="FrontSelected" />

        <shared:SharedImagePicker Grid.Column="2"
                                  HorizontalAlignment="Center"
                                  Title="Reverso"
                                  ImageUrl="{Binding Entity.ImageBackFullPath}"
                                  Icon="{x:Static icons:FontAwesomeIcon.IdCard}"
                                  ImageSelected="BackSelected" />

        <shared:SharedModalActions Grid.Row="2" Grid.ColumnSpan="3"
                                   SaveCommand="{Binding SaveCommand}"
                                   CancelCommand="{Binding CancelCommand}" />

        <shared:SharedLoading Grid.RowSpan="3" Grid.ColumnSpan="3" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
