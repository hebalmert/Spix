<UserControl x:Class="Spix.AppWpf.Views.EntitiesContratos.ContractClient.EditContractClientDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Spix.AppWpf.Views.EntitiesContratos.ContractClient"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents"
             MinWidth="960">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="26" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <local:ContractClientFormView />

        <shared:SharedModalActions Grid.Row="2"
                                   SaveCommand="{Binding SaveCommand}"
                                   CancelCommand="{Binding CancelCommand}" />

        <shared:SharedLoading Grid.RowSpan="3" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
