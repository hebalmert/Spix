<UserControl x:Class="Spix.AppWpf.Views.EntitiesPayment.ContractExonerated.EditContractExoneratedDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Spix.AppWpf.Views.EntitiesPayment.ContractExonerated"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents">

    <!-- Editar una exoneracion programada. Mismo formulario que el alta, mas el bloque de
         facturado. Guardar la re-cotiza con el precio del plan de hoy. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <local:ContractExoneratedFormView />

        <shared:SharedModalActions Grid.Row="2"
                                   SaveCommand="{Binding SaveCommand}"
                                   CancelCommand="{Binding CancelCommand}" />

        <shared:SharedLoading Grid.RowSpan="3" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
