<UserControl x:Class="Spix.AppWpf.Views.EntitiesPayment.ContractorCxC.ContractorCxCPaymentsDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:catalog="clr-namespace:Spix.AppWpf.SharedComponents.SharedCatalog"
             xmlns:shared="clr-namespace:Spix.AppWpf.SharedComponents"
             MinWidth="880">

    <!-- Los abonos que se le han hecho a la cuenta, del mas reciente al mas viejo.
         El saldo de cada renglon es el que quedo DESPUES de ese abono. -->
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Margin="0,0,0,14"
                Padding="14,10"
                CornerRadius="12"
                Background="{DynamicResource BrushCatalogCard}"
                BorderBrush="{DynamicResource BrushCatalogKpiBorder}"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Style="{StaticResource SharedIndexName}" Text="{Binding ContractorName}" />
                    <TextBlock Style="{StaticResource SharedIndexMeta}" Text="{Binding DateNoteText}" />
                </StackPanel>

                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{DynamicResource BrushCatalogKpiAccent}"
                           Text="{Binding NoteNumber}" />
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,14">
            <catalog:SharedCatalogKpi Margin="0,0,10,0"
                                      Label="Total"
                                      Value="{Binding Total, StringFormat=N2}" />
            <catalog:SharedCatalogKpi Margin="0,0,10,0"
                                      Label="Abonado"
                                      Value="{Binding Paid, StringFormat=N2}"
                                      Accent="{DynamicResource BrushCatalogKpiOk}"
                                      ValueBrush="{DynamicResource BrushCatalogKpiOk}" />
            <catalog:SharedCatalogKpi Label="Saldo"
                                      Value="{Binding Balance, StringFormat=N2}"
                                      Accent="{DynamicResource BrushCatalogKpiBad}"
                                      ValueBrush="{DynamicResource BrushCatalogKpiBadText}" />
        </UniformGrid>

        <Border Grid.Row="2" MinHeight="160" Style="{StaticResource SharedIndexPanel}">
            <Grid>
                <Border Style="{StaticResource SharedIndexHeaderBand}" />

                <DataGrid ItemsSource="{Binding Rows}"
                          Style="{StaticResource SharedIndexGrid}"
                          ColumnHeaderStyle="{StaticResource SharedIndexHeader}"
                          RowStyle="{StaticResource SharedIndexRow}"
                          CellStyle="{StaticResource SharedIndexCell}">

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="160" Header="Fecha" SortMemberPath="DatePaymentText">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SharedIndexName}" Text="{Binding DatePaymentText}" />
                                        <TextBlock Style="{StaticResource SharedIndexMeta}" Text="{Binding UsuarioOwner}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Width="150" Header="Modo de pago" SortMemberPath="ModeName">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource SharedIndexMeta}" Text="{Binding ModeName}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Width="*" MinWidth="200" Header="Referencia" SortMemberPath="Reference">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SharedIndexName}" Text="{Binding Reference}" />
                                        <TextBlock Style="{StaticResource SharedIndexMeta}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Text="{Binding Detail}"
                                                   ToolTip="{Binding Detail}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Width="130" Header="Monto" SortMemberPath="Payment">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Right"
                                               Style="{StaticResource SharedIndexName}"
                                               Text="{Binding Payment, StringFormat=N2}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Width="130" Header="Saldo" SortMemberPath="Balance">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Right"
                                               Style="{StaticResource SharedIndexMeta}"
                                               Text="{Binding Balance, StringFormat=N2}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource SharedIndexPanelFoot}">
            <shared:SharedPagination Foreground="{DynamicResource BrushCatalogMeta}"
                                     CurrentPage="{Binding CurrentPage}"
                                     TotalPages="{Binding TotalPages}"
                                     PageCommand="{Binding GoToPageCommand}" />
        </Border>

        <shared:SharedModalActions Grid.Row="4"
                                   Margin="0,20,0,0"
                                   ShowSave="False"
                                   CancelCommand="{Binding CancelCommand}" />

        <shared:SharedLoading Grid.RowSpan="5" IsLoading="{Binding IsLoading}" />
    </Grid>
</UserControl>
