<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="clr-namespace:FontAwesome.Net.Wpf;assembly=FontAwesome.Net.Wpf">
    <Style x:Key="SharedButtonTableRow" TargetType="Button">
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource BrushRowActionForeground}" />
        <Setter Property="Background" Value="{StaticResource BrushTableRowAction}" />
        <Setter Property="Tag" Value="{StaticResource BrushButtonSecondaryHover}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushRowActionBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="ToolTipService.ShowDuration" Value="5000" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="18">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="Background"
                                    Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
                            <Setter TargetName="ButtonBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="10"
                                                      ShadowDepth="3"
                                                      Opacity="0.55"
                                                      Color="#000000" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource BrushDisabled}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
                            <Setter Property="Cursor" Value="Arrow" />
                            <Setter Property="Opacity" Value="0.7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SharedButtonTableRowEdit" TargetType="Button" BasedOn="{StaticResource SharedButtonTableRow}">
    </Style>

    <Style x:Key="SharedButtonTableRowDelete" TargetType="Button" BasedOn="{StaticResource SharedButtonTableRow}">
        <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
        <Setter Property="Background" Value="{StaticResource BrushTableRowDelete}" />
        <Setter Property="BorderBrush" Value="#DC2626" />
        <Setter Property="Tag" Value="{StaticResource BrushTableRowDeleteHover}" />
    </Style>

    <Style x:Key="SharedButtonTableRowIcon" TargetType="fa:FontAwesomeImage">
        <Setter Property="Foreground"
                Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
    </Style>

    <Style x:Key="SharedButtonTableRowDetails" TargetType="Button" BasedOn="{StaticResource SharedButtonTableRow}">
    </Style>
</ResourceDictionary>
