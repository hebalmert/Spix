<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SharedButtonForm" TargetType="Button">
        <Setter Property="MinWidth" Value="144" />
        <Setter Property="Height" Value="42" />
        <Setter Property="Padding" Value="20,0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
        <Setter Property="Background" Value="{StaticResource BrushButtonPrimary}" />
        <Setter Property="Tag" Value="{StaticResource BrushButtonPrimaryHover}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="Background"
                                    Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ButtonBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="14"
                                                      ShadowDepth="4"
                                                      Opacity="0.65"
                                                      Color="#000000" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource BrushDisabled}" />
                            <Setter Property="Cursor" Value="Arrow" />
                            <Setter Property="Opacity" Value="0.7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SharedButtonFormPrimary" TargetType="Button" BasedOn="{StaticResource SharedButtonForm}">
        <Setter Property="Background" Value="{StaticResource BrushButtonPrimary}" />
        <Setter Property="Tag" Value="{StaticResource BrushButtonPrimaryHover}" />
    </Style>

    <Style x:Key="SharedButtonFormSecondary" TargetType="Button" BasedOn="{StaticResource SharedButtonForm}">
        <Setter Property="Background" Value="{StaticResource BrushButtonSecondary}" />
        <Setter Property="Tag" Value="{StaticResource BrushButtonSecondaryHover}" />
    </Style>

    <Style x:Key="SharedButtonFormSuccess" TargetType="Button" BasedOn="{StaticResource SharedButtonForm}">
        <Setter Property="Background" Value="{StaticResource BrushButtonSuccess}" />
        <Setter Property="Tag" Value="{StaticResource BrushButtonSuccessHover}" />
    </Style>

    <Style x:Key="SharedButtonFormDanger" TargetType="Button" BasedOn="{StaticResource SharedButtonForm}">
        <Setter Property="Background" Value="{StaticResource BrushButtonDanger}" />
        <Setter Property="Tag" Value="{StaticResource BrushButtonDangerHover}" />
    </Style>
</ResourceDictionary>
